# 设计问题清单

## 审核问题表

| 检查维度 | 问题描述 | 严重等级 | 是否存在 | 是否解决 | 关联成果物 | 关联需求编号 | 建议责任角色 | 修复建议 |
| -------- | -------- | -------- | -------- | -------- | ---------- | ------------ | ------------ | -------- |
| 需求覆盖度 | | 高/中/低 | | | | | | |
| 目标达成性 | | 高/中/低 | | | | | | |
| 功能 | | 高/中/低 | | | | | | |
| 体验 | | 高/中/低 | | | | | | |
| 可行性 | | 高/中/低 | | | | | | |
| MVP 范围 | | 高/中/低 | | | | | | |
| 任务可执行性 | | 高/中/低 | | | | | | |
| 流程合规性 | | 高/中/低 | | | | | | |
| 架构设计原则 | | 高/中/低 | | | | | | |
| 成果物完整性 | | 高/中/低 | | | | | | |
| 测试可执行性 | | 高/中/低 | | | | | | |
| 安全与合规 | | 高/中/低 | | | | | | |

> **填写规则（R18，机读）**：上表 **12 个检查维度行须全部保留**。无问题时「是否存在」填 `否`。存在问题时：「是否存在」=`是`、「是否解决」在修复前填 `否`；**关联成果物 / 关联需求编号 / 建议责任角色 / 修复建议** 均须非空（流程类问题「关联需求编号」可填 `无`）。建议责任角色须为可识别角色名（如 `system-architect` / `系统架构师`）。

## 需求覆盖矩阵

> **须覆盖** `requirement-list.md` 全部 **P0** 需求（R18 机读硬门禁）。每行「验收标准」「设计落点」「**设计落点原文摘录**」「任务包」须非空；「覆盖结论」仅允许 `已覆盖` / `未覆盖`。设计落点须能在 `detail-design-spec.md` 解析到对应章节（非 stub 时）；任务包须为可识别编号（如 `T0-1`）且出现在 `develop-task-list.md`（非 stub 时）。任一 P0 为 `未覆盖` 时，须在上方「需求覆盖度」登记未解决问题，且不得进入开发。
>
> **「设计落点原文摘录」列（R18 机读必填列，语义相关性仍由人工核验）**：设计落点的机械校验仅为弱正则/子串匹配（章节号在设计文档中出现即视为可解析，不校验内容是否真实相关，见 `.cursor/harness/spec/mechanical-gates.md` §8.4）。Hook **强制**本列存在且每行非空；请在本列**摘录该章节里与本条验收标准直接相关的一句原文**（而非整段复制），供 QE/PM 复核时快速判断落点是否名实相符；不得留空或摘录与本需求无关的句子。

| 需求编号 | 优先级 | 验收标准 | 设计落点 | 设计落点原文摘录 | 任务包 | 覆盖结论 |
| -------- | ------ | -------- | -------- | ---------------- | ------ | -------- |
| R-001 | P0 | AC-R-001-1 或验收断言摘要 | detail-design-spec.md §x | 「……」（摘自 §x 的相关原句） | T0-1 | 已覆盖/未覆盖 |

## 审核结论

> **R18 机读**：须由 `requirement-reviewer` 填写。首次无问题通过填「通过」；若审核问题表中存在「是否存在」=`是` 且已「是否解决」=`是` 的返工行，最新结论**必须**为「复审通过」，否则不得进入开发。系统架构师返工时**禁止**改写本节结论。

| 审核轮次 | 结论 | 说明 |
| -------- | ---- | ---- |
| 1 | 通过/不通过/复审通过 | 首次审核或复审说明 |
